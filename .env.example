# SMTP Email Configuration (Optional)
# Without these variables, the site works perfectly but emails won't be sent
# Orders will still be processed and displayed to users

# Gmail Example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Outlook/Hotmail Example:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# Analytics (Optional)
# Only loads if PLAUSIBLE_DOMAIN is set
PLAUSIBLE_DOMAIN=yourdomain.com

# Site URL (for sitemap and SEO)
NEXT_PUBLIC_SITE_URL=https://eyekhel.com